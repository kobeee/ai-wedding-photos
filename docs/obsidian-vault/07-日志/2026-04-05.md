---
tags: [日志, VPS验证, Harness, V13, 双轨管线]
created: 2026-04-05
related:
  - "[[04-研发笔记/V12-SSS出片目标与双轨Harness工程]]"
  - "[[05-开发/后端开发]]"
  - "[[06-运维/部署Runbook]]"
---

# 2026-04-05

## V13 链路对齐修正 + VPS 实跑验证

### 本轮修正

- `src/backend/context/thresholds.py`
  - `meets_delivery_floor()` 改为按 `track` 生效
  - hero 轨最终交付底线真正使用 `HERO_MIN_DELIVERY_FLOOR=0.80`
- `src/backend/services/production_orchestrator.py`
  - hero / face_lock 可见性判定统一改走 `meets_delivery_floor(..., track="hero")`
- `src/backend/routers/generate.py`
  - 订单交付层只落 `user_visible=true` 的资产
  - 隐藏的 `validation-safe` 继续保留在 `result_assets`，不再混入 deliverables / deliverable_count
  - `delivered_photos` 改为只统计用户可见资产
- `src/backend/tests/test_order_closure.py`
  - 旧测试桩从一步法入口切换到 `production_orchestrator_service.run_photo`

### 本地回归

- `python3 -m unittest src/backend/tests/test_context_engineering.py src/backend/tests/test_verification_architecture.py`
  - `29 tests OK`
- `python3 -m unittest src/backend/tests/test_order_closure.py`
  - `3 tests OK`
- `cd src/frontend && npm run build`
  - 前端构建通过

### VPS 部署

- `rsync` 同步到 `/opt/apps/wedding-photos/`
- `docker compose -p wedding-photos -f docker/docker-compose.yml build`
- `docker compose -p wedding-photos -f docker/docker-compose.yml up -d --force-recreate`
- 容器恢复后：
  - `http://127.0.0.1:3080/api/health` → `200`
  - `http://127.0.0.1:3080/api/health/detail` 显示：
    - `vlm_max_tokens=4096`
    - `nano_api_configured=true`
    - `chat_api_configured=true`

### 实跑链路

使用 VPS 本地标准采集样片真实跑通：

`upload -> makeup(male/female) -> generate -> status/result -> public file download`

#### Upload

- 使用 5 张标准采集图上传：
  - `couple_full`
  - `groom_portrait`
  - `groom_full`
  - `bride_portrait`
  - `bride_full`
- 返回：
  - `user_id = 74249d2bd73e`
  - `session_token = lst_2be76c4b3e8244048d8433fffe418e3b`
- `validation.ok = true`
- AI 上传校验仍出现一次 JSON 截断，自动回退 heuristic，但不阻断建档

#### Makeup

- `POST /api/makeup/generate` `male` → `200`
- `POST /api/makeup/generate` `female` → `200`
- 两路都返回 3 张试妆图，说明：
  - session 鉴权正常
  - Nano Banana 试妆链路正常

#### Generate

- `POST /api/generate` 成功创建：
  - `task_id = 42ba0caa4e524536`
- 说明：
  - 直接传 `gender="couple"` 会被 schema 拒绝
  - 当前双人生成仍依赖“不传 gender，后端回退到 couple”这条兼容语义

#### 运行日志要点

- 参考图筛选日志：
  - `Reference selection: 3 primary, 0 auxiliary from 5 candidates`
- 明确进入最新双轨编排，不是旧一步法：
  - validation 轨连续多轮重试
  - 日志里出现 `Validation track attempt failed`
  - 失败原因覆盖：
    - `Face area ratio ... below floor 0.025`
    - `body visibility ... below floor 0.80`
- 后续继续推进到 hero 轨并完成 4K 交付：
  - `Delivery image prepared ... -> 4096`

#### 最终结果

- `GET /api/generate/42ba0caa4e524536/status` →
  - `status = completed`
  - `message = 生成完成，validation-safe 1 张，hero 1 张，综合评分 0.91`
- `GET /api/generate/42ba0caa4e524536/result` →
  - `result_urls` 只有 1 张可见图
  - `result_assets` 包含：
    - 1 张隐藏 `validation-safe`，`user_visible=false`
    - 1 张可见 `hero-atmosphere`，`track=hero`，`quality_score=0.91`
- 公网下载验证：
  - `GET https://wedding.escapemobius.cc/api/files/outputs/74249d2bd73e/0af4d933977d.png`
  - 携带 `Authorization` 后返回 `200`
  - 下载体积约 `8.9MB`

### 结论

- 线上已经切到最新 V13 主链路
- 这次修正后，隐藏的 validation 资产不会再被订单交付层误当成用户交付片
- hero 轨最终交付门槛已真正按 V13 生效
- VPS 真实试跑已完成，当前可继续在 VPS 上按最新链路做后续验证

### 仍需后续跟进

- `GenerateRequest.gender` schema 仍不接受 `couple`，目前靠“不传 gender”兼容双人场景
- `allow_degraded_delivery_on_vlm_unavailable=true` 仍显示在健康接口里，但当前 orchestrator 实际并不会在 validation / hero 质检不可用时继续交付

## Waiting 进度修复 + 订单复查

### 本轮修正

- `src/backend/services/production_orchestrator.py`
  - 新增 `progress_callback`
  - validation / hero / face-lock / final packing 都会上报子阶段进度与文案
- `src/backend/routers/generate.py`
  - 把 orchestrator 的子阶段进度映射回单张照片的订单批次进度区间
  - 长耗时重试时也会更新 message，不再只停在 `正在生成第 1/N 张...`
- `src/backend/tests/test_order_closure.py`
  - 新增回归测试，确保单张照片生成期间存在 `>12%` 的中间进度更新

### 回归与部署

- `python3 -m py_compile src/backend/services/production_orchestrator.py src/backend/routers/generate.py`
  - 通过
- `python3 -m unittest src/backend/tests/test_order_closure.py`
  - `4 tests OK`
- `python3 -m unittest src/backend/tests/test_context_engineering.py src/backend/tests/test_verification_architecture.py`
  - `29 tests OK`
- VPS 重新部署后，公网健康恢复：
  - `https://wedding.escapemobius.cc/api/health` → `{"status":"ok","service":"lumiere-studio","api_configured":true}`

### 线上验证结果

- 旧订单 `ord_910047d013b149f1`
  - 仍指向旧批次 `batch_78ff861afec54fcb`
  - 当前库内状态仍是：
    - `status=processing`
    - `progress=12`
    - `message=正在生成第 1/3 张...`
    - `updated_at=2026-04-05 04:47:53`
  - 结论：这不是“还在跑”，而是部署前旧进程遗留的中断批次
- 新建真实试跑订单：
  - `order_id = ord_82cfd709b3d14f93`
  - `batch_id = batch_2fcfb22dcdd64dc3`
  - 轮询结果确认已经越过旧卡点：
    - `progress=14`
    - `message=第 1/3 张: 正在验证构图与身份第 1/5 次...`
    - `updated_at=2026-04-05 05:26:33`

### 结论

- 等待页“12% 长时间无反馈”这一段主链路已修复并已上线
- 当前仍可能先在 `8%` 停留一段时间，因为试妆参考图解析 / 生成仍发生在进入首张照片前
- 如果要继续优化体感，下一步应把 `resolve_selected_makeup_reference()` 这段前置耗时也拆成可见进度

## 卡死订单强制清理

- 用户确认放弃旧订单 `ord_910047d013b149f1`，要求直接清理 VPS 数据重新开始
- 已在线上执行：
  - 删除 `orders / generation_batches / deliverables / payment_transactions / order_scene_selections` 中该订单关联记录
  - 恢复该订单原绑定体验码为 `active`
  - 清空该身份 `c86287169c16` 的 `/app/uploads` 与 `/app/outputs` 目录
- 最终复核：
  - `orders=0`
  - `batches=0`
  - `deliverables=0`
  - `payments=0`
  - `scene_selections=0`
- 新补发体验码：
  - `LUMIERE-R8N6C4QW`
  - `/api/experience-codes/verify` 返回 `valid=true`

## 严重性结论（留待下个 Session）

- 当前问题已确认为**系统级问题**，不是单一前端展示 bug
- 严重性判断：
  - `Waiting` 百分比卡住只是表象
  - 真正风险是后台任务被部署/中断后，订单批次仍可能长期留在 `processing`
  - 这会制造“假运行中”状态，用户无法自行恢复
- 已知事实：
  - `ord_910047d013b149f1` 就是典型悬挂批次案例
  - 新补的子阶段进度已证明 `12%` 这段可以继续推进
  - 但前置 `8%` 阶段仍存在较长不可见耗时
  - 更关键的是，系统当前没有“中断任务自动收敛”的恢复机制
- 下个 Session 应优先讨论：
  - 批次状态机补全
  - 悬挂批次回收/失败转移
  - 前置耗时步骤显式进度化
